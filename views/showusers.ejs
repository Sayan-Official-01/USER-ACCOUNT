<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show</title> 
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css"
    rel="stylesheet">
    <style>
        body {
            padding-top: 10px;
            padding-left: 10px;
        }
        table,th, td{
            border: 3px solid black;
        }
    </style>
   
</head>
<body>
    <table>
        <tr>
            <th>Id</th>
            <th>Username</th>
            <th>User</th>
            <th>Edit</th>
        </tr>
        <tr>
            <% for(user of users){ %>
                <tr>
                    <td><%= user.id %></td>
                    <td><%= user.username %></td>
                    <td><%= user.email %></td>  
                    <td>
                        <form method="GET" action="http://localhost:8080/user/<%= user.id %>/edit">
                            <button class="btn btn-outline-success btn-lg">Edit username</button>
                        </form>
                    </td> 
                    <td>
                        <form method="POST" action="/user/<%= user.id %>?_method=DELETE">
                            <button class="btn btn-outline-danger btn-lg" type="submit">Delete</button>
                        </form>
                    </td>                  
                </tr>
            <% } %>
        </tr>
    </table>
</body>
</html>







